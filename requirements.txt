# FORMA — Python Dependencies
# Generated: 2026-03-10 (AC-0 spike)
# Python: 3.14.0 (M3 Mac, arm64)
# Environment: forma-env (python -m venv forma-env)
#
# Install order matters:
#   pip install pygarment shapely ezdxf trimesh scipy smplx pytest warp-lang
#
# Known NOT installable on Python 3.14 / M3 Mac:
#   open3d         — no py3.14 wheel
#   pygarment>=2.0 — cgal dependency has corrupt PyPI metadata
#   NvidiaWarp-GarmentCode fork — requires Python ≤3.11 + CUDA + GCC to build
#
# For full GPU simulation (Colab A100 / Linux + CUDA):
#   git clone https://github.com/maria-korosteleva/NvidiaWarp-GarmentCode
#   cd NvidiaWarp-GarmentCode && python build_lib.py && pip install -e .
#   (requires Python 3.10-3.11, CUDA 11.5+, GCC 7.2+)
#
# SMPL-X model files (not pip-installable — must download manually):
#   https://smpl-x.is.tue.mpg.de/
#   Place at: data/smplx_models/SMPLX_MALE.npz

# Core garment pattern framework
pygarment==1.0.0.post1

# Geometry / mesh
shapely==2.1.2
ezdxf==1.4.3
trimesh==4.11.3

# Numeric / scientific
scipy==1.17.1
numpy==2.4.3

# SMPL-X body model (fallback path — BetaSolver)
smplx==0.1.28
torch==2.10.0

# Warp physics (base JIT layer; sim module requires GarmentCode fork)
warp-lang==1.12.0

# Testing
pytest==9.0.2

# pygarment transitive dependencies (pinned for reproducibility)
CairoSVG==2.8.2
cairocffi==1.7.1
cffi==2.0.0
cssselect2==0.9.0
defusedxml==0.7.1
tinycss2==1.5.1
webencodings==0.5.1
pillow==12.1.1
matplotlib==3.10.8
PyYAML==6.0.3
svgwrite==1.4.3
svgpathtools==1.7.2
psutil==7.2.2
PySimpleGUI==5.0.8.3
contourpy==1.3.3
cycler==0.12.1
fonttools==4.62.0
kiwisolver==1.5.0
packaging==26.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0

# smplx / torch transitive dependencies
sympy==1.14.0
mpmath==1.3.0
networkx==3.6.1
fsspec==2026.2.0
filelock==3.25.1
Jinja2==3.1.6
MarkupSafe==3.0.3

# pytest transitive dependencies
iniconfig==2.3.0
pluggy==1.6.0
Pygments==2.19.2

# misc
typing_extensions==4.15.0
setuptools==82.0.1
pycparser==3.0
